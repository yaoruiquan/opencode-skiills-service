# =============================================================================
# OpenCode Skills Service — 环境变量模板
# 复制为 .env 后填入真实值
# =============================================================================

# ---- OpenCode 镜像 ----
# OpenCode 容器镜像，默认使用 GitHub Container Registry 最新版
OPENCODE_IMAGE=ghcr.io/anomalyco/opencode:latest

# ---- Provider 配置（DeepSeek Anthropic 兼容 API） ----
# Provider 标识，用于 OpenCode 内部路由
OPENCODE_PROVIDER_ID=deepseek-anthropic
OPENCODE_PROVIDER_NAME=DeepSeek Anthropic
# 对应 @ai-sdk 的 provider 包名
OPENCODE_PROVIDER_NPM=@ai-sdk/anthropic
# DeepSeek Anthropic 兼容接口地址（使用 /anthropic 路径）
OPENCODE_API_BASE_URL=https://api.deepseek.com/anthropic
# DeepSeek API key（从 platform.deepseek.com 获取）
OPENCODE_API_KEY=replace-with-your-deepseek-api-key

# ---- 默认模型 ----
# OpenCode 执行时使用的模型 ID = {provider_id}/{model}
OPENCODE_MODEL=deepseek-v4-flash
# 前端展示用名称
OPENCODE_MODEL_DISPLAY_NAME=DeepSeek V4 Flash
# 是否启用思考模式（DeepSeek V4 Flash 不支持 reasoning=true）
OPENCODE_MODEL_REASONING=false
# 上下文窗口大小（token）
OPENCODE_MODEL_CONTEXT=128000
# 最大输出 token 数
OPENCODE_MODEL_OUTPUT=64000

# ---- Skills 目录 ----
# 宿主机 skills 目录路径，通过 bind mount 挂入容器
# 本地开发：/Users/yao/.claude/skills
# 服务器：  /opt/opencode-skills/skills
SKILLS_HOST_DIR=/Users/yao/.claude/skills

# ---- Chrome DevTools MCP（Docker Chrome 链路） ----
# 三个独立 Docker Chrome 服务，分别用于 CNVD / CNNVD / NCC 上报
# HOST 为容器内 Docker service name，PORT 为容器内调试端口
CHROME_DEVTOOLS_CNVD_HOST=browser-cnvd
CHROME_DEVTOOLS_CNNVD_HOST=browser-cnnvd
CHROME_DEVTOOLS_NCC_HOST=browser-ncc
CHROME_DEVTOOLS_CNVD_PORT=9332
CHROME_DEVTOOLS_CNNVD_PORT=9333
CHROME_DEVTOOLS_NCC_PORT=9334

# ---- Skills API ----
# skills-api 服务监听端口（默认 4100）
SKILLS_API_PORT=4100
# job 目录根路径（容器内）
SKILLS_API_JOBS_ROOT=/data/work/jobs

# ---- Fallback 与重试 ----
# OpenCode 首选模型 capacity 满时，依次尝试的 fallback 模型列表（逗号分隔）
OPENCODE_FALLBACK_MODELS=deepseek-anthropic/deepseek-v4-pro
# 首选模型 capacity 重试次数（0 = 不重试立即切 fallback）
OPENCODE_CAPACITY_RETRIES=1
# 重试间隔（毫秒）
OPENCODE_CAPACITY_RETRY_DELAY_MS=5000
